spec:
  name: electric-sync-service
  services:
    - name: electric
      image:
        registry_type: DOCKER_HUB
        registry: electricsql
        repository: electric
      health_check:
        period_seconds: 60
      http_port: 3000
